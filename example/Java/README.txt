A small console app that posts a UTF-8 encoded xml document to ecrud transform.

How to Use:
- If needed change localURL to the url of the server you want to test against. 
- Run/debug it.
- The app will make two POSTs one passing a string the other an InputStream, both responses sent to System.out.println()